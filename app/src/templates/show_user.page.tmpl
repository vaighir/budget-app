{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <p>{{index .StringMap "username"}}</p>
            </div>
        </div>
    </div>
{{end}}